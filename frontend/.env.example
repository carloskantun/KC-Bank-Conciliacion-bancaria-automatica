# Variables del frontend (Next.js).
# Únicamente variables públicas necesarias para el navegador.
# NUNCA agregar aquí credenciales de PostgreSQL, Redis, MinIO ni del backend.
#
# Uso:
#   cp .env.example .env.local

NEXT_PUBLIC_API_URL=http://localhost:3001
